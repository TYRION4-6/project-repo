{
  "name": "shipbasket-portal",
  "version": "1.0.0",
  "description": "Unified Multi-Branch Shipbasket Platform with Modern Theme & Animations",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --open\"",
    "start": "npx concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --open\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
